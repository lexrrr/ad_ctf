html
    head
        title SecretChannel
        style
          include style.css
    body
      div.content
        h1 Wrong password
        p You used the wrong password, please try again.
        p Hint: use the password for your token (ID: #{token.id}, action: #{token.action})
        form(
          action='/'
          method='POST'
          )
          label(for='token') Token:
          input(placeholder='Insert your token' type='text' name='token' value=token_enc)
          label(for='pw') Password:
          input(placeholder='Insert token password' type='password' name='pw')
          input(type='submit' value='Start')
